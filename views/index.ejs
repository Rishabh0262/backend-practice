<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <!-- If no recipe yet, show: -->
    <h2>Pick your favourite taco ingredient 👆</h2>
    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
    <h2><%= dataVal.name %></h2>
    <h3>Ingredients:</h3>
    <ul id="ingredientsList">
      <%# var protein = ''  %>
      <li><%= dataVal.ingredients.protein.name  %> </li>
      <li><%= dataVal.ingredients.salsa.name %></li>
      <% 
      // for (let topping in dataVal.ingredients.toppings) { 
        const topping = dataVal.ingredients.toppings;

        console.log(topping)
        const toppingsLen = topping.length;
        for (let i =0; i<toppingsLen; i++) {

        
        %>

        <li> <%= topping[i].quantity %> of <%= topping[i].name %> </li>

      
      <% } %>
    </ul>



  </div>
</body>

</html>